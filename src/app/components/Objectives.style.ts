'use client';
import styled from 'styled-components';

export const ObjectivesContainer = styled.section`
  border-left: 5px solid var(--accent);
  padding-left: 1.5rem;
  margin: 3rem 0;

  ol {
    padding-left: 1rem;
  }

  li {
    margin-bottom: 1rem;
  }
`;